import * as objects from './objects';
import { notes } from './objects';
//importar por nombre con llaves {}
//sin llaves solo el default

// const addNoteButton = document.createElement('button');
// document.body.appendChild(addNoteButton);

// addNoteButton.addEventListener('click', () => {
// 	const newNote = new objects.Note(objects.notes.length || 0);
// 	objects.notes.push(newNote);
// 	objects.Storage.save(notes);
// 	console.log(notes);
// });
